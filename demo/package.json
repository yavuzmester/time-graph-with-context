{
  "name": "demo",
  "version": "1.0.0",
  "dependencies": {
    "react": "^15.0.2",
    "react-dom": "^15.0.2",
    "@yavuzmester/time-graph-with-context": "file:..",
    "browserify": "^13.0.0"
  },
  "scripts": {
    "rebuild": "rm -rf build && mkdir build && browserify app.js -o build/bundle.js"
  }
}
